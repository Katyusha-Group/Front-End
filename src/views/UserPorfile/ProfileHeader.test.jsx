import { render } from "@testing-library/react";
import ProfileHeader from "./ProfileHeader";
import { test, it } from "vitest";

test("ProfileHeader component", () => {
  // it("should render the rightUpper component", () => {
  //   const { getByTestId } = render(<ProfileHeader />);
  //   expect(getByTestId("rightUpper")).toBeInTheDocument();
  // });
});
test("ProfileHeader component", () => {
  // it("should render the ProfileImage component", () => {
  //   const { getByTestId } = render(<ProfileHeader />);
  //   expect(getByTestId("ProfileImage")).toBeInTheDocument();
  // });
});
test("ProfileHeader component", () => {
  // it("should render the name component", () => {
  //   const { getByTestId } = render(<ProfileHeader />);
  //   expect(getByTestId("name")).toBeInTheDocument();
  // });
});
test("ProfileHeader component", () => {
  // it("should render the myusername component", () => {
  //   const { getByTestId } = render(<ProfileHeader />);
  //   expect(getByTestId("myusername")).toBeInTheDocument();
  // });
});
test("ProfileHeader component", () => {
  // it("should render the follow component", () => {
  //   const { getByTestId } = render(<ProfileHeader />);
  //   expect(getByTestId("follow")).toBeInTheDocument();
  // });
});
