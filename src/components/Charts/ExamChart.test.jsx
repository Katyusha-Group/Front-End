import { render } from "@testing-library/react";
import ExamChart from "./ExamChart";
import { test, it } from "vitest";

test("ExamChart component", () => {
  // it("should render the ExamChart table component", () => {
  //   const { getByTestId } = render(<ExamChart />);
  //   expect(getByTestId("ExamChartTable")).toBeInTheDocument();
  // });
});
test("ExamChart component", () => {
  // it("should render the TimeRow component", () => {
  //   const { getByTestId } = render(<ExamChart />);
  //   expect(getByTestId("ExamChartTimeRow")).toBeInTheDocument();
  // });
});
